import math

# importing the `sqrt` and `pi` functions from the `math`
# module.
from math import sqrt, pi

# importing everything
from math import *

# This allows you to refer to the module using the alias `ttk` instead of its full name
# `ttkbootstrap` in your code.
import ttkbootstrap as ttk

# The code `import Avi` is importing a module named `Avi`. The module `Avi` likely contains some
# functions or classes that can be used in the code.
import Avi

Avi.welcome()

print(Avi.avi)


# The `print(dir(ttk))` statement is printing the list of attributes and methods available in the
# `ttk` module. The `dir()` function returns a sorted list of names in the given module, and by
# passing `ttk` as an argument to `dir()`, we can see all the available attributes and methods in the
# `ttk` module.

# print(dir(ttk))




# result = sqrt(9)
# print(result)  # Output: 3.0


# print(pi)  # Output: 3.141592653589793
